#!/bin/sh

pse()
{
	ps -ax | sort -bn
}

# $ ./aaa
# $ pse
# これだと何も起こらない

# $ . ./aaa
# $ pse
# これだと実行できる

# ドット(.)というシェルの組み込み関数を使用してシェルスクリプトを実行させると、中に含まれているコマンドは、現在のシェル（シェルスクリプトを起動したシェル）上で動作することになる。
