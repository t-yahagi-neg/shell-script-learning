#!/bin/bash

echo $$

# シェルスクリプトで処理途中のデータを一時的なファイルに出力したい時
# そのファイル名を tmp.txt など固定の名前にしてしまうと
# そのシェルスクリプトを複数同時に実行した時に
# 複数のプロセスから１つのファイルに書き込むことになり、
# ファイルの内容が壊れてしまう
# このような場合には、次のように一時ファイル名に$$を含めておく
# tempfile=/tmp/script_temp$$
